# Five-Minute Demonstration

1. Open the EAI web workspace and show the current case list.
2. Ask the Copilot agent to show current customer cases and compare `CASE-1042` across both channels.
3. Ask Copilot to create a medium-priority customer case. Highlight the confirmation prompt before the record is written.
4. Open the new record in the web workspace to show one shared system of record.
5. Change the case status and show the version increasing from `1` to `2`.
6. Explain that EAI External ID can include external customers and partners while PublicAPI and Authz preserve tenant membership and permissions.

Do not claim a live platform or Copilot result when running synthetic mode. The page identifies its current mode explicitly.
